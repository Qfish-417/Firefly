# FireFly QuestLab — 简历介绍

所有数字来自 `docs/evaluation-report.md` 的实测记录，可用该文档末尾的命令复现。
被追问时按文末「追问准备」回答。

## 一句话版本

自进化教育 Agent 平台的架构与 RAG。3.4 万行 TypeScript、12 个包、40 张表、47 份 ADR；
真实文档语料上 Recall@10 从 0.706 优化至 **0.912**，端到端五维综合 **0.979**，
过程中定位并修复 5 个生产环境会出问题的检索缺陷。

## 简历条目（可直接粘贴）

格式沿用你简历里已有的写法：标题 + 「相关技术栈」一行 + 每条「小标题：做了什么，量化结果」。

---

### 推荐版本

> **FireFly QuestLab 自进化教育 Agent 平台　架构与 RAG 负责人**
>
> 相关技术栈：TypeScript / Node 24 / PostgreSQL 17 + pgvector / HNSW + halfvec / 混合检索（BM25 + 向量 + RRF 融合）/ JSON Schema 契约校验 / Outbox-Inbox / 事件溯源 / Docker Compose / 本地部署 Qwen3
>
> **三 Agent 协作架构设计**：设计学习总监、学习科学家、体验工程师的协作边界——Agent 间不通过自由文本接力、不互改状态，Control Plane 以版本化 `Task` 派活、Agent 以 append-only `Event` 报告事实、大对象走 `ArtifactRef`，PostgreSQL 工作流状态为唯一事实源，保证每个教学决策可回放、可回滚；落地 12 个包、40 张表、18 个迁移、47 份 ADR，共 3.4 万行 TypeScript
>
> **检索质量量化与优化：Recall@10 从 0.706 提升至 0.912**：先修正评测口径（gold set 由"整个文件"改为 h1–h4 小节，1~8 个 chunk，对齐 MS MARCO 量级），再按"排名分布定位瓶颈 → 逐项实测"推进；实测排除 6 条无效杠杆（候选窗口 24→200 仅 +1.3% 即饱和、兄弟 chunk 扩展 -2.7%、RRF 常数与双腿权重全部落在 0.764~0.776），据此判定瓶颈不在融合层而在索引侧；修复后词法腿 0.543 → 0.868，检索 p50 299ms
>
> **真实文档语料暴露并修复 5 个缺陷**：英文自然句词法检索 96/96 零结果致混合检索静默退化为向量单腿（MRR 0 → 0.724）；证据层把同文件段落当重复丢弃、每来源仅留 1 条（Recall@10 0.054 → 0.202、引用精确率 0.232 → 0.480）；chunk 丢失标题上下文致多 chunk 小节召回仅 0.502（→ 0.692）；Markdown 表格查询因 `-` 被解析为 NOT 操作符压爆解析栈而抛异常；重排 provider 忽略 `top_k` 致整条重排链路不可用
>
> **Agent 能力五维评测体系**：刻意让五个维度事实来源互不共享（工作流事实表 / 状态迁移数 / 生产校验函数 / `model_invocation` 审计账本 / 独立 LLM 裁判），避免单一指标掩盖问题；端到端综合 0.979——任务完成 6/6、步效率 9/9 理论最小迁移、工具调用正确率 0.952、1507 token/run（基线 3000）、模型调用 0 失败 0 重试、0 契约违规；270 项测试全绿

---

### 精简版（版面只够 3 条时）

删掉「三 Agent 协作架构设计」，保留后三条。理由：架构描述最容易被追问细节且不易量化，而后三条
每一条都带可复现的数字，是这个项目独有的、问不倒的内容。

---

### 极简版（一行）

> **FireFly QuestLab 自进化教育 Agent 平台**（TypeScript / PostgreSQL + pgvector / 混合检索）：
> 三 Agent 契约优先架构，3.4 万行、40 张表、47 份 ADR；真实文档语料上 Recall@10 从 0.706 优化至
> 0.912，过程中定位修复 5 个生产级检索缺陷；端到端五维综合 0.979，270 项测试全绿。

---

## 写作说明（为什么这么改）

优化前后的差别，供你判断是否接受：

| 项 | 改法 | 理由 |
|---|---|---|
| 「R@10 达上限的 43%」 | 删掉 | 「达上限」是我自创的指标，不是通用口径，面试官需要额外解释才能看懂 |
| 「优化了检索效果」 | 改为「0.706 → 0.912」 | 你已有简历里全是「~5s 降至 ~0.5s」「+73%」这类写法，保持一致 |
| 「使用了 RRF 融合」 | 改为「实测排除 6 条无效杠杆，据此判定瓶颈在索引侧」 | 会用某个技术不稀缺；能证明某条路走不通、并据此改变方向，才是判断力 |
| 缺陷描述 | 保留具体机制（`-` 被解析为 NOT、`top_k` 被忽略） | 这类细节无法编造，是最有说服力的部分 |
| 五维评测 | 强调「事实来源互不共享」 | 说明你知道评测本身会骗人，而不只是「跑了个评测」 |

**建议主动讲的一点**：这个项目里我推翻过自己 6 条结论（见下表）。面试时主动说出来通常是加分项，
因为它说明数字是测出来的而不是套用的结论。

## 追问准备

### Q：R@10 = 0.912 是怎么算的？

`前 10 条命中数 / gold set 大小`。除以 10 得到的是 Precision@10 —— 这两个必须分开，
如果 Recall 也除以 10，它会与 Precision 位位相同（这个 bug 项目里真出现过：11 场景 × 3 cutoff
全部 `Recall@k == Precision@k`）。

gold set 按 h1–h4 小节划分，实测 4058 个小节里 99% 含 1–5 个 chunk。此前用"同一文件的全部 chunk"
是错的：`big-integer/README.md` 的 58 个 chunk 全被算成与 "how do I use big integer" 相关，
包括 `#### shiftLeft(n)`，它与查询无任何词汇或语义重叠。分母虚高一个量级，R@10 被压到 0.274。

### Q：0.912 里有没有作弊 / 标签泄漏？

有条件，我在报告里明写了。文档标识的收益依赖查询提供来源（"在 X 文档里找 Y"），查询去掉来源后
收益消失（0.783 vs 无标识的 0.799）。

判定它不是泄漏的三条证据：标识只到*文件*层而 gold set 是 (文件, 小节)，exceljs 的 100+ 小节共享
同一标识；标题唯一组 0.914 与标题重复组 0.905 几乎相同（若只是消歧，重复组才该独涨）；R@1 仍只有
0.565，说明仍需在同文件上百小节中排序。

中途另有一个 0.863 的做法（从查询解析来源、给同源 chunk 加权）被我放弃了——那是给 RRF 外挂特判，
且依赖我自己构造的查询格式。

### Q：为什么引用精确率只有 0.218？

这是两个目标的固有冲突，不是缺陷。gold set 均值 1.34 而证据包给 8.28 条，算术上限约
1.34/8.28 = 0.162，实测 0.218 就在上限附近。最大化 R@10 要求返回 10 条，最大化引用精确率要求
只给 1–2 条。两个数一起报，让权衡可见。

### Q：重排为什么关掉？换个重排器会不会是正收益？

在**本项目条件下**实测有害：0.910 → 0.875，还多花约 120ms，所以默认关闭。

但我不会把它说成「重排没用」，因为这个结论有明确的适用边界，两个条件都成立才成立：

1. **用的不是专用重排器**。这里是 embedding 模型兼挂的 `/v1/rerank`，不是专门训练的
   cross-encoder（如 `bge-reranker-v2-m3` 那类）。换成专用重排器需要重测。
2. **基线已接近饱和**。修完索引侧缺陷后基线已是 0.910，重排能补的边际信息很少。若基线在 0.4~0.6
   区间，重排通常有明显收益。

跑这个实验时还顺带发现本地 `/v1/rerank` 忽略请求里的 `top_k`（请求 3 条、给 5 篇文档，返回 5 条），
而客户端断言"恰好 top_k 条"，导致整条重排链路对这类 provider 完全不可用——尽管它的打分本身正确。
已改成"至少 top_k 条 + 客户端按分数降序自己截断"。

### Q：哪些结论被你自己的测量推翻了？

报告第 0.5 节专门记录，这是我认为最该读的部分：

| 曾经的结论 | 推翻它的证据 |
|---|---|
| 质量随语料规模上升 | 相关集随语料一起变大造成的假象；固定相关集后 16 倍规模差异下 nDCG 0.680→0.687 |
| halfvec 有 31% 精度损失 | 两个探针 bug（`SET LOCAL` 事务外静默无效、ANN 索引未按 `index_version_id` 分区）；修正后 fp16 舍入召回 1.000，损失全来自 HNSW 近似 |
| 自适应 k 能提升召回 | 相关/不相关候选的 `score/best` 分布几乎重合（p50 0.520 vs 0.492），RRF 归一化比不携带相关性信号 |
| 召回差是查询用词与文档不一致 | 换成文档标题当查询，召回从 52% **降到** 31% |
| HyDE 能提升检索 | 全局重写使词法 P@10 降 14%、p50 增 958ms；只作零结果兜底才是净收益 |
| 满意度可由 LLM 裁判代表 | 独立标注与裁判 `completeness` 的 Pearson r 仅 0.248 |

### Q：已知的不足？

- 跨主体聚合缺失：`t.ambig.cross-subject` 3/3 失败。检索合同只支持单主体计数与两主体比较，
  正确行为是报告能力缺失，模型却回退到 `retrieval/exploratory`。这是边界不是回归（clear 档 1.000）。
- ANN 索引未按 `(tenant_id, index_version_id)` 分区；`hnsw.iterative_scan` 未接进产品代码。
- 检索身份 HMAC 无 nonce 且未绑定请求体，±30s 内可重放。
- 满意度只有 LLM 打分，缺真人标注。

---

## 技术关键词（用于 JD 匹配）

RAG、混合检索（BM25 + 向量 + RRF 融合）、pgvector / HNSW / halfvec、重排、
Recall@k / Precision@k / nDCG / MRR、contextual chunking、LLM-as-judge、
多 Agent 编排、契约优先设计（JSON Schema）、Outbox/Inbox、事件溯源、
TypeScript、Node 24 原生 TS、PostgreSQL 17、Docker Compose、审计账本与预算控制
