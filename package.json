{
  "name": "firefly-questlab",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --test \"packages/**/*.test.ts\"",
    "test:integration": "node --test \"packages/**/*.integration.test.ts\"",
    "db:migrate": "node packages/persistence/src/migrate.ts",
    "check": "npm run typecheck && npm test"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/pg": "^8.15.5",
    "typescript": "^5.9.0"
  }
}
